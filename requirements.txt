# PyTorch — visit https://pytorch.org/get-started/locally/ to get the right
# CUDA wheel for your driver. Example for CUDA 12.1:
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
torch
torchvision

# Scientific / data
scipy
matplotlib
Pillow>=9.2.0

# Vision / ML
einops
vector-quantize-pytorch

# Models
git+https://github.com/openai/CLIP.git
# 1d-tokenizer is not pip-installable — clone it manually into the project root:
#   git clone https://github.com/bytedance/1d-tokenizer.git

# Evaluation
torchmetrics[image]

# HuggingFace
datasets
huggingface_hub

# Misc
omegaconf
accelerate

# ---------------------------------------------------------------------------
# Model checkpoints — download into the ckpts/ folder at the project root
# ---------------------------------------------------------------------------
#
# 1. MaskGIT VQ-GAN tokenizer (required for training and inference)
#
#    python -c "
#    from huggingface_hub import hf_hub_download
#    hf_hub_download(repo_id='fun-research/TiTok',
#                    filename='maskgit-vqgan-imagenet-f16-256.bin',
#                    local_dir='./ckpts')
#    "
#
#    Or with wget:
#    mkdir -p ckpts
#    wget -P ckpts https://huggingface.co/fun-research/TiTok/resolve/main/maskgit-vqgan-imagenet-f16-256.bin
#
# 2. imagenet_256_L.ckpt
#    TODO: add source repo / HuggingFace link
